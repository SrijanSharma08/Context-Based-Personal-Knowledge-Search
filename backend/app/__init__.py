"""
Backend application package for the Personal Knowledge Organizer.

This module simply marks `app` as a package; all functionality lives in the
submodules such as `config`, `ingestion`, `embeddings`, `vectorstore`, `llm`,
`rag`, and `models`.
"""

